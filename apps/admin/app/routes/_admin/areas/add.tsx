import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '~/components/ui'

export default function AreaAddPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Add new Area</CardTitle>
        <CardDescription>Add a new area to the system</CardDescription>
      </CardHeader>
      <CardContent>hoge</CardContent>
    </Card>
  )
}
